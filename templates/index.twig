{% extends "_layout" %}

{% block content %}
    
    <div class="section section--default">
        <div class="container">
            <div class="w-full md:w-3/4">
                {% include '_snippet/_content/_intro' %}
            </div>
        </div>
    </div>
    
{% set details = craft.entries.section('bronDetail') %}
    {% for detail in details %}
        <a href="{{ detail.url() }}">klikken</a>
    {% endfor %}

{% endblock %}
